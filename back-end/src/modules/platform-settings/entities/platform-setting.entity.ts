// In-memory store project — no TypeORM decorators.
// Re-export the PlatformSetting interface for type safety across the module.
export type { PlatformSetting } from '../../../common/database/database.service';
