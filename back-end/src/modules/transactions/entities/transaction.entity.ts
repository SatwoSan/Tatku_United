// In-memory store project — no TypeORM decorators.
// Re-export the Transaction interface for type safety across the module.
export type { Transaction } from '../../../common/database/database.service';
